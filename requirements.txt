# Core (required) -----------------------------------------------------------
numpy>=1.24
scipy>=1.10

# Pose estimation for real videos (optional but recommended) ----------------
mediapipe>=0.10.9
opencv-python>=4.8

# Highest-accuracy backend (optional) ---------------------------------------
# RTMPose via rtmlib — use with `--backend rtmpose`. Most accurate keypoints.
rtmlib>=0.0.13
onnxruntime>=1.16

# Plotting (optional) -------------------------------------------------------
matplotlib>=3.7

# Vision-language report providers (optional) -------------------------------
openai>=1.30
google-generativeai>=0.7
python-dotenv>=1.0

# Dev -----------------------------------------------------------------------
pytest>=7.4
