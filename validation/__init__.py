"""LANDR real-data accuracy validation harness.

Validates LANDR's angle measurements against the OpenCap lab dataset (synchronized
phone video + marker motion capture). See ``docs/VALIDATION.md`` for the plan and
the anti-overfitting protocol.
"""
